node_modules/
.DS_Store
*.log

# only the standalone single-file build is committed
dist/artifact.html

# the local server's records — one learner's own data, not source
server/data/

# patch files handed over from a chat session — applied, never committed
claude-patches/
